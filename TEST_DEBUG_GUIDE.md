# 🔍 测试调试指南

## 当前问题

测试运行时提示：`No test results found`

这通常意味着：
1. ✅ 测试文件被找到
2. ❌ 测试代码在运行时崩溃
3. ❌ 测试框架无法解析测试结果

---

## 📋 诊断步骤

### 步骤1：查看崩溃日志

**在 DevEco Studio 中：**

1. 找到底部的 **Log** 窗口
2. 切换到 **FaultLog** 标签页
3. 查找错误信息，关键词：
   - `jscrash`
   - `Error`
   - `Exception`
   - `Cannot find module`

**请将看到的错误信息发给我！**

---

### 步骤2：查看 Hilog 日志

1. 在 DevEco Studio 底部找到 **Hilog** 窗口
2. 在搜索框输入：`TestSuite` 或 `BasicTests`
3. 查看是否有测试相关的日志输出

---

### 步骤3：运行系统自带的测试

**验证测试框架是否正常：**

1. 打开：`entry/src/ohosTest/ets/test/Ability.test.ets`
2. 右键点击文件 → **Run 'abilityTest'**
3. 查看是否能正常运行

**如果系统测试能运行：**
- 说明测试框架正常
- 问题在我们的自定义测试中

**如果系统测试也失败：**
- 说明测试环境配置有问题

---

## 🐛 可能的问题和解决方案

### 问题1：导入路径错误

**症状：** FaultLog 中显示 `Cannot find module`

**原因：** 测试文件的相对导入路径不正确

**解决：**
```typescript
// 检查路径是否正确
import { GameService } from '../../../main/ets/services/GameService';

// 路径层级：
// ohosTest/ets/test/ -> 上一级 -> 上一级 -> 上一级 -> entry/src/main/ets/
```

### 问题2：Mock 对象不完整

**症状：** FaultLog 中显示 `Cannot read property xxx of undefined`

**原因：** 测试中使用的 Mock 对象缺少必要的方法

**解决：** 完善测试文件，简化测试逻辑

### 问题3：异步测试处理不当

**症状：** 测试超时或异步代码未完成

**解决：** 增加超时时间或使用 `done` 回调

---

## ✅ 立即可做的测试

### 方案1：手动测试核心算法

由于自动化测试遇到问题，我们可以先手动验证算法：

**在 DevEco Studio 的控制台中：**

1. 打开 `GameService.ets`
2. 在 `evaluateGuess` 方法中添加日志：
```typescript
console.log('密码:', password.join(','));
console.log('猜测:', guess.join(','));
console.log('结果:', `hits=${result.hits}, pseudoHits=${result.pseudoHits}`);
```

3. 运行应用，进行一次游戏
4. 提交猜测后查看 Hilog 输出

### 方案2：创建单元测试（本地验证）

**创建一个独立的 Node.js 脚本：**

```javascript
// /tmp/test_algorithm.js
function evaluateGuess(guess, password) {
  const hits = guess.filter((color, index) => color === password[index]).length;

  const guessRemaining = [];
  const passwordRemaining = [];

  for (let i = 0; i < guess.length; i++) {
    if (guess[i] !== password[i]) {
      guessRemaining.push(guess[i]);
      passwordRemaining.push(password[i]);
    }
  }

  let pseudoHits = 0;
  for (let i = 0; i < guessRemaining.length; i++) {
    const color = guessRemaining[i];
    const index = passwordRemaining.indexOf(color);
    if (index !== -1) {
      passwordRemaining.splice(index, 1);
      pseudoHits++;
    }
  }

  return { hits, pseudoHits };
}

// 测试用例
const guess = ['blue', 'green', 'red', 'yellow'];
const password = ['red', 'yellow', 'green', 'blue'];
const result = evaluateGuess(guess, password);

console.log('密码:', password);
console.log('猜测:', guess);
console.log('结果:', result);
console.log('预期: hits=0, pseudoHits=4');
console.log('测试通过:', result.hits === 0 && result.pseudoHits === 4);
```

运行：
```bash
node /tmp/test_algorithm.js
```

---

## 📊 当前状态总结

### ✅ 已完成
- [x] 创建完整的测试用例设计文档
- [x] 创建测试执行指南
- [x] 创建测试清单
- [x] 创建测试代码（虽然运行有困难）

### ⏳ 待解决
- [ ] 诊断测试框架运行失败的原因
- [ ] 修复测试环境配置问题
- [ ] 成功运行测试套件

### 🎯 建议

**优先级1：手动验证算法**
- 在真机/模拟器上运行游戏
- 添加日志输出
- 验证 ALG-003 场景是否正确

**优先级2：查看 FaultLog**
- 在 DevEco Studio 中查看崩溃日志
- 将错误信息反馈给我

**优先级3：简化测试**
- 从最简单的测试开始
- 逐步增加复杂度
- 找出导致崩溃的具体代码

---

## 💬 请您提供以下信息

为了更好地帮助您，请提供：

1. **FaultLog 中的错误信息**
   - 打开 DevEco Studio → Log → FaultLog 标签页
   - 复制所有红色/橙色的错误信息

2. **BasicTest 的运行结果**
   - 尝试运行 `BasicTest.test.ets`
   - 告诉我是否仍然显示 "No test results"

3. **Ability.test.ets 的运行结果**
   - 尝试运行系统自带的测试
   - 是否能正常运行

有了这些信息，我就能准确定位问题并修复！
